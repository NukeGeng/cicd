# cicd

## Bài thực hành 10

Project React dùng cho mô phỏng CI/CD với GitHub và Jenkins.

```bash
cd my-react-app
npm install
npm run build
```

- macOS/Linux: `bash scripts/deploy.sh`
- Windows/Jenkins: `scripts\deploy.bat`
